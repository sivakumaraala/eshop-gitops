apiVersion: v1
kind: Service
metadata:
  name: ordering-api
  namespace: dev
  labels:
    app.kubernetes.io/name: ordering-api
    app.kubernetes.io/part-of: test-microservice
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ordering-api
  ports:
    - port: 80
      targetPort: http
      name: http
