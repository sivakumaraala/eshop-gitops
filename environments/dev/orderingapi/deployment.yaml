apiVersion: apps/v1
kind: Deployment
metadata:
  name: ordering-api
  namespace: dev
  labels:
    app.kubernetes.io/name: ordering-api
    app.kubernetes.io/part-of: test-microservice
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: ordering-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ordering-api
        app.kubernetes.io/part-of: test-microservice
    spec:
      containers:
        - name: ordering-api
          image: YOUR_ACR_NAME.azurecr.io/eshop/ordering.api:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: ordering-api-config
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /alive
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
