apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: dev
  labels:
    app.kubernetes.io/name: catalog-api
    app.kubernetes.io/part-of: test-microservice
spec:
  selector:
    app.kubernetes.io/name: catalog-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
