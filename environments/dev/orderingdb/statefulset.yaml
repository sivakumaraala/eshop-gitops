apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: orderingdb
  namespace: dev
  labels:
    app.kubernetes.io/name: orderingdb
    app.kubernetes.io/part-of: test-microservice
spec:
  serviceName: orderingdb
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: orderingdb
  template:
    metadata:
      labels:
        app.kubernetes.io/name: orderingdb
        app.kubernetes.io/part-of: test-microservice
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_DB
              value: orderingdb
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_HOST_AUTH_METHOD
              value: trust
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres -d orderingdb
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres -d orderingdb
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
