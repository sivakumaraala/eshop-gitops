apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: prod
  labels:
    app.kubernetes.io/name: webapp
    app.kubernetes.io/part-of: test-microservice
spec:
  selector:
    app.kubernetes.io/name: webapp
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP

